{
  "functions": {
    "src/app/api/**/*": {
      "memory": 3008,
      "maxDuration": 60
    }
  },
  "crons": [{
    "path": "/api/cron/fetch-tweets",
    "schedule": "*/200 * * * *"
  }],
  "buildCommand": "bash -c 'if [ -n \"$NEXT_PUBLIC_URL\" ] && [ -n \"$CRON_SECRET\" ]; then curl -X GET -H \"Authorization: Bearer $CRON_SECRET\" \"$NEXT_PUBLIC_URL/api/init/fetch-tweets\"; fi && next build'"
} 
